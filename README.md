# Codexion
Codexion is a 42 School project (dining philosophers variant) implementing a multithreaded C simulation where coders compete for shared USB dongles to compile code. It uses POSIX threads and mutexes to handle synchronization, deadlock prevention, and burnout detection.
